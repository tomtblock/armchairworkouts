[build]
  command = "pnpm build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9.15.9"

# The Netlify Next.js plugin handles routing automatically
# No manual redirects needed when using the plugin

